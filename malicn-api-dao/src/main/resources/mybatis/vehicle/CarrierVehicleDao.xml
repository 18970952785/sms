<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.malicn.server.dao.vehicle.CarrierVehicleDao" >

	<resultMap id="vehicleDriverMap" type="com.malicn.server.domain.vehicle.CarrierVehicle">
		<id column="id" property="id" />
		<result column="id" property="id" />
		<result column="carrier_id" property="carrierId" />
		<result column="vehicle_id" property="vehicleId" />
		<result column="relation_type" property="relationType" />
		<result column="create_time" property="createTime" />
		<result column="update_time" property="updateTime" />
	</resultMap>

	<sql id="Base_Column_List" >
		id, carrier_id, vehicle_id, relation_type, create_time, create_time, is_deleted
	</sql>
	
	<!-- 新增操作 -->
	<insert id="saveVehicleDriver" parameterType="com.malicn.server.domain.vehicle.CarrierVehicle" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO ml_carrier_vehicle
		(carrier_id, vehicle_id, relation_type, create_time, update_time)
		VALUES
		(#{carrierId}, #{vehicleId}, #{relationType}, now(), now())
	</insert>

	<select id="getByCarrIdAndVhlId" resultMap="vehicleDriverMap">
		SELECT  <include refid="Base_Column_List" />
		FROM ml_carrier_vehicle
		WHERE carrier_id = #{carrId} AND vehicle_id = #{vhlId}
	</select>

	<select id="getByCarrIdAndDriverId" resultMap="vehicleDriverMap">
		select cv.id,cv.carrier_id,cv.vehicle_id,cv.relation_type,cv.create_time,cv.create_time
		from ml_carrier_vehicle cv,ml_vehicle v
		where  v.driver_id =#{driverId} and cv.carrier_id=#{carrId}  and cv.vehicle_id  = v.id
		limit 1;
	</select>
	
	<!-- 修改操作 -->
	<update id="updateVehicleDriver" parameterType="com.malicn.server.domain.vehicle.CarrierVehicle" >
		UPDATE ml_carrier_vehicle
		<set>
			<if test="carrierId != null">
				carrier_id = #{carrierId},
			</if>
			<if test="vehicleId != null">
				vehicle_id = #{vehicleId},
			</if>
			<if test="relationType != null">
				relation_type = #{relationType},
			</if>
			update_time = now()
		</set>
		<where>
			id = #{id}
		</where>
	</update>
	<delete id="delete" parameterType="java.lang.Long">
		DELETE FROM ml_carrier_vehicle
		WHERE id = #{id}
	</delete>

	<delete id="deleteByPersonalDriver">
		DELETE FROM ml_carrier_vehicle
		WHERE carrier_id = #{carrierId} and vehicle_id = #{vehicleId}
	</delete>

	<select id="getCarrierExpectYULIIJUN" resultMap="vehicleDriverMap">
		select
			max(t.carrier_id) as carrier_id
		from ml_carrier_vehicle t
			where t.vehicle_id=#{vehicleId}
		limit 1
	</select>


	<update id="updateCarrierIdByIds">
		update ml_carrier_vehicle set carrier_id = #{newDriverId}
		where carrier_id = #{oldDriverId} and vehicle_id =#{vehicleId} ;
	</update>


</mapper>